import { cn } from '@/util/util'
import React from 'react'

function AboutMe({ className }) {
    return (
        <div className={cn(className, 'bg-green-300 flex items-center justify-center')}>

        </div>
    )
}

export default AboutMe