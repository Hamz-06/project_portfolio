import React from 'react'
import { cn } from '@/util/util'
function Contacts({ className }) {
    return (
        <div className={cn(className, 'bg-fuchsia-700')}>Contacts</div>
    )
}

export default Contacts